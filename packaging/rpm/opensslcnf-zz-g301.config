# Explicit experimental laboratory opt-in. This is not a native Fedora
# subpolicy and makes no FIPS, BSI or EMPTY-policy claim.
[g301_policy_capture]
base_ciphersuites = ${crypto_policy::Ciphersuites}

[crypto_policy]
Ciphersuites = G301-AES-256-GCM-V1:${g301_policy_capture::base_ciphersuites}
