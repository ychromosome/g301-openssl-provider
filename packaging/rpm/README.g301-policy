The policy subpackage contains `opensslcnf-zz-g301.config.example` as an inert
laboratory example. It does not install under `/etc/crypto-policies/local.d`
and does not change Fedora's selected policy. Fedora's `.pmod` vocabulary has
no entry for the private G301 TLS ciphersuite.
